{% extends 'base.html.twig' %}

{% block title %}Etablissements{% endblock %}

{% block body %}


<div class="container pt-5">

    <h1>Les établissements</h1>
    <hr>

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

    <div class="row">
        {% for etablissement in pagination %}

            {% if etablissement.estActif %}

                {% include "partials/etablissements/card.html.twig" %}

            {% endif %}
        {% endfor %}
    </div>

    <div class="navigation my-3">
        {{ knp_pagination_render(pagination) }}
    </div>

</div>


{% endblock %}
